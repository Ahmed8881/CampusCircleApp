<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.CampusCircleApp" parent="Theme.CampusCircleApp.SeckhoLight" />

    <style name="Theme.CampusCircleApp" parent="Base.Theme.CampusCircleApp" />

    <style name="Theme.CampusCircleApp.Splash" parent="Theme.CampusCircleApp">
        <item name="android:windowBackground">?attr/android:colorBackground</item>
    </style>
</resources>
